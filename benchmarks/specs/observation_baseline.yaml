experiment: observation_flow
params:
  width: 50
  height: 50
  steps: 10
  num_agents: 20
  radius: 3
  fov_radius: 3
  device: cpu
iterations:
  warmup: 0
  measured: 1
instrumentation:
  - timing
  - cprofile
  - psutil
output_dir: benchmarks/results
tags: [perception, baseline]
notes: "Baseline CPU observation throughput"
seed: 42

