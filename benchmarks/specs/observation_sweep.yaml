experiment: observation_flow
params:
  width: 200
  height: 200
  steps: 30
  device: cpu
sweep:
  num_agents: [50, 100]
  radius: [5, 6]
iterations:
  warmup: 1
  measured: 2
instrumentation:
  - timing
output_dir: benchmarks/results/obs_sweep
tags: [sweep, perception]
notes: "Small sweep for observation throughput"
seed: 123

