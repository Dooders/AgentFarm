experiment: memory_db
params:
  num_agents: 100
  num_steps: 100
  parameters:
    test_in_memory: true
    test_disk_based: true
    test_persistence: true
iterations:
  warmup: 1
  measured: 2
instrumentation:
  - timing
  - psutil
output_dir: benchmarks/results/memory_db_baseline
tags: [database, baseline]
notes: "Memory database performance baseline"
seed: 42
