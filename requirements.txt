# Core dependencies
numpy>=1.24.0,<2.3.0  # NumPy 2.3+ incompatible with Numba (Tianshou dependency)
scipy>=1.7.0  # For spatial algorithms (KDTree) and statistical analysis
pandas>=1.3.0
matplotlib>=3.4.0
pyyaml>=6.0
sqlalchemy>=1.4.0
pydantic>=2.0.0  # Data validation and serialization
deepdiff>=5.8.0
pettingzoo>=1.24.0  # Multi-agent reinforcement learning environments

# Database
# Note: sqlite3 is part of Python standard library, no need to install separately
redis>=4.0.0  # Redis client for memory management

# GUI
# Note: tkinter is part of Python standard library, no need to install separately
pillow>=9.0.0  # For PIL/Image processing in GUI
pywin32>=228; sys_platform == 'win32'  # For Windows clipboard operations

# Visualization
snakeviz>=2.1.0  # For profiling visualization
seaborn>=0.11.0  # Statistical visualizations

# Testing
pytest>=6.2.5
pytest-cov>=4.0.0

# Utilities
tqdm>=4.62.0  # Progress bars
colorama>=0.4.4  # Terminal colors
python-dotenv>=0.19.0  # Environment variables
psutil>=5.8.0  # System and process utilities (CPU, memory monitoring)

# Logging
structlog>=24.1.0  # Structured logging
python-json-logger>=2.0.0  # JSON log formatting

# Machine Learning
# For PyTorch, choose one of:
torch>=2.0.0  # Default PyTorch with CUDA support if available
# torch>=2.0.0  # CPU-only PyTorch (smaller, no CUDA)
# Install PyTorch CPU with: pip install torch -f https://download.pytorch.org/whl/torch_stable.html
scikit-learn>=1.3.0  # For machine learning utilities
joblib>=1.2.0  # Model serialization
xgboost>=1.7.0  # Gradient boosting (optional)
lightgbm>=3.3.0  # Alternative gradient boosting (optional)

# Data Analysis
networkx>=2.6.0  # For network analysis
plotly>=5.3.0  # Interactive visualizations
dash>=2.0.0  # Dashboard for visualization
statsmodels>=0.14.0  # Statistical modeling and time series analysis

# Reinforcement Learning
tianshou>=0.4.11,<0.5.0  # Tianshou RL library (PPO, SAC, A2C, DQN, DDPG, etc.) - pinned due to NumPy compatibility
gymnasium>=0.28.0  # Modern Gym replacement for environments
shimmy>=1.0.0  # Environment wrappers for compatibility
stable-baselines3>=2.0.0  # Reinforcement learning algorithms

# Web API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
httpx>=0.24.0  # Required for FastAPI test client

# LLM Integration
openai>=1.0.0  # OpenAI API client for chart analysis
pyscn>=0.1.0  # PySCN library

# Optional dependencies
moviepy>=1.0.3  # For video generation and processing
# Note: tkinter is part of Python standard library, no need to install separately  
